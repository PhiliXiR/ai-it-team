# Prompt Placeholders

This folder is intended for future normalized prompt/spec files.

The project is not yet committing to a specific runtime format.
For now, prompt conventions are documented in:

- `docs/implementation/prompt-conventions.md`
- `docs/agent-spec-format.md`
